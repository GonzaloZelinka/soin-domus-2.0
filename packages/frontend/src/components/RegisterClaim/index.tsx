import React from 'react'
import { IParams } from 'shared-common'
interface Props {
  refInfo: IParams
}
const RegisterClaim = ({ refInfo }: Props) => {
  return <div>refInfo</div>
}

export default RegisterClaim
